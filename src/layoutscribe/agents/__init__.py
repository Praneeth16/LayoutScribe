"""Agent graph and nodes for LayoutScribe (stubs).

Submodules (planned):
- `graph.py`: LangGraph graph wiring of planner → page_vision → composer → reviewer
- `planner.py`: pre-processing, page queue planning, DPI/tiling decisions
- `page_vision.py`: single-page vision LLM call to produce layout JSON
- `composer.py`: blocks → Markdown/plain text
- `reviewer.py`: schema/geometry checks, re-asks, aggregation
"""


