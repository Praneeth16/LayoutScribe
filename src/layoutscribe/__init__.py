"""LayoutScribe package initialization.

This package provides an LLM-only, agentic pipeline to parse documents
like PDF, PPTX, and DOCX into Markdown, plain text, and structured
layout JSON. Public entrypoints will be exposed from `api.py` and a CLI
from `cli.py` once implemented.
"""

__version__ = "0.1.0a3"

__all__ = [
  "__version__",
]


